{
  "name": "nightshade-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "2.*",
    "bluebird": "3.*",
    "dotenv": "^8.0.0",
    "express": ">=4.17.3",
    "express-session": "1.*",
    "mysql": "2.*",
    "passport": "0.*",
    "passport-local": "1.*",
    "pug": "2.*"
  }
}
